# DESIGN.md walk — remaining phases

Brand & Style + voice: locked.

**Implementation target:** Pico CSS v2 custom theme (`archanglic.pico.css`) + vanilla `.border-beam`. Not React.

1. **Colors** (now) — husk slots, not Geist accents. `.working/color-husk-slots.md`. Wire each pick into `--arch-*` / `--pico-*`.
2. **Typography** (scheduled next) — dedicated phase. `/ux-palette` used `ui-sans-serif, system-ui`; Goddy did not love those. Do not treat that stack as the identity. Live-site typeface remains a keeper to *show* in that phase, not auto-lock from the palette page. Pico `--pico-font-family-*` waits on this phase.
3. Layout & Spacing
4. Elevation & Depth (no shadow stacks; beam knobs still open)
5. Shapes
6. Components (Pico atoms + Archanglic domain)
7. Do's and Don'ts

Finalize distills spines after Discovery closes.
