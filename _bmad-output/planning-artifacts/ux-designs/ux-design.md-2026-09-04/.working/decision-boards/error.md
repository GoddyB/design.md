# Husk `error` — LOCKED

Do not reopen.

**`#eb1000`** — Goddy pick **c / write-in**. Red somewhere. Not a Geist red ladder. Not `{colors.primary}` recycled as angry.

Pico: `--arch-error` / `--pico-del-color` / `--pico-form-element-invalid-*`.

Next: [`queued.md`](./queued.md) — `on-error`. Do not invent that lock.
