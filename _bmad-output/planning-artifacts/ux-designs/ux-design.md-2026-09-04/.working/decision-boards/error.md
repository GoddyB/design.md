# Husk `error` — answer this one

Rapid-fire. One slot. Do not reopen `primary` or `on-primary`.

**Look at the invalid line (a, c) or the Delete fill (b).** That color is the token. Must be red somewhere. Not a Geist red ladder. Not `{colors.primary}` recycled as angry.

Open [index.html](./index.html) for the live columns (they side-scroll; they do not stack).

## Usage

```
token name
[________________]
 ! This token does not resolve. You cannot ship it.
   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   this color
```

or, if you pick the pill job:

```
[ Delete ]
  ^^^^^^
  this FILL
```

Paws: error is its own palette, never primary recycled as “angry.” Goddy: errors need red.

## a / b / c

The word “Archanglic” is always `#ededed`. Fat strip is the token swatch.

| | Hex | Role | vs `#000` | vs `#ededed` |
|---|---|---|---|---|
| **a** | `#ffb4ab` | Atmospheric/Totality dark-theme error — **text on black** | 12.37:1 AA | 1.45:1 fail as fill under type-white |
| **b** | `#ba1a1a` | Paws error — **filled Delete** | 3.25:1 fail as text-on-black | 5.52:1 AA as fill under type-white |
| **c** | `#c45c5c` | muted rust, still clearly red — **text on black** | 5.03:1 AA | 3.56:1 fail as fill under type-white |

a and c are line-on-void. b is a pill. Pick the job, then the hex.

```
     a #ffb4ab              b #ba1a1a              c #c45c5c
    +----------------+     +----------------+     +----------------+
    | invalid line   |     | [ Delete ]     |     | invalid line   |
    |  text on void  |     |  filled pill   |     |  text on void  |
    +----------------+     +----------------+     +----------------+
```

Pico: `--pico-del-color` / `--pico-form-element-invalid-*` / `--arch-error`. Placeholder is `gray-500` until you pick.

## Reply

```
error: a
```

or `b` / `c` / a write-in hex that is not a Geist accent ladder and is not void primary painted “angry.”

Then stop. Next slot is `on-error`.
